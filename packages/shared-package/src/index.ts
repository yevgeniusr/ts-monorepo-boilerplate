// Export your shared utilities, types, and constants here

export const getSharedMessage = (): string => {
  return 'This message comes from the shared @ts-monorepo-boilerplate/common package!';
};

export const example = () => {
  return 'Hello from shared package';
};
