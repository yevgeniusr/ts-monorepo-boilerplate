{
  "name": "@ts-monorepo-boilerplate/common",
  "version": "0.0.0",
  "type": "module",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "?build": "This package is links directly to its source files",
    "type-check": "tsc --noEmit",
    "type:check": "tsc --noEmit",
    "test": "vitest",
    "coverage": "vitest run --coverage",
    "lint": "eslint . --max-warnings 0",
    "clean": "rm -rf dist node_modules .turbo"
  },
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@ts-monorepo-boilerplate/eslint-config": "workspace:*",
    "@ts-monorepo-boilerplate/typescript-config": "workspace:*",
    "eslint": "8",
    "eslint-plugin-require-extensions": "^0.1.3",
    "prettier": "^3.2.5",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
